- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.emacs.d:
    ~/.emacs:

- shell:
    - git submodule sync --recursive
    - git submodule update --init --recursive
    - >
      git clean -f
      emacs.d/vendor/
